// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

/// @title CounterStorage
/// @notice Proxy와 Logic이 공유하는 저장소 레이아웃
/// @dev 업그레이드 시 스토리지 충돌을 방지하기 위해 분리
contract CounterStorage {
    // =============================================================
    // 스토리지 슬롯 (순서 절대 변경 금지!)
    // =============================================================

    // 슬롯 0: 카운터 값
    uint256 internal _count;

    // 슬롯 1: 소유자
    address internal _owner;

    // =============================================================
    // 중요: 새 변수는 반드시 아래에 추가해야 함!
    // 기존 변수의 순서를 바꾸면 데이터가 깨짐
    // =============================================================
}
